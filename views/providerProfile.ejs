<h2>Provider Page</h2>

<div class='profile'>
<h3>Welcome to your Provider PROFILE</h3>


<p>Id: <%= id %></p>
<p>Name: <%= name %></p>
<p>Email: <%= email %></p>

<p>Provider Number:</p>
<span id="copyText"><%= providerNumber %></span>
<div>
    <button type="button"  class="btn btn-primary" onclick="copyLink()" data-toggle="tooltip" title="Copy to Clipboard" readonly >Copy Code</button>
</div>
<script>
  function copyLink() {
    let copyText = document.getElementById("copyText") 

    var selection = window.getSelection();

    var range = document.createRange();

    range.selectNodeContents(copyText);

    selection.removeAllRanges();

    selection.addRange(range);

    document.execCommand('copy');
  }
</script>
</div>

<h2>Edit your credentials</h2>
<form action="/auth/edit/<%=id%>/?_method=PUT" method="POST">
    <div class="form-group">
        <label for="new-email">Email</label>
        <input id="new-email" type="email" name="email" class="form-control" placeholder="<%=email%>">
    </div>
    <div class="form-group">
        <label for="new-name">Name</label>
        <input id="new-name" type="text" name="name" class="form-control" placeholder="<%=name%>">
    </div>
    <button type="submit" value="Update" class="btn btn-primary">Update</button>
  </form>
<br>
